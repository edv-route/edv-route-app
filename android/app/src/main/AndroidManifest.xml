<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Network calls to the EDV backend (release talks HTTPS to Railway). -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Location while working (proposal: ubicacion-afiliados). COARSE goes with
         FINE because Android 12+ lets the user grant only the approximate one,
         and half a position beats none. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- "All the time". Android 10+ asks for this SEPARATELY, in a second prompt
         that lands in system settings — it is where most people fall out of the
         flow, so the app explains why before asking. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>

    <!-- Android kills background work within minutes. The only way to keep
         reporting with the app closed is a foreground service, and its permanent
         notification is also the honest signal to somebody being located. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <!-- Keeps the CPU awake long enough to take a fix and post it. -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Brings tracking back after a reboot: a driver who restarts his phone
         mid-shift should not vanish off the map until he happens to notice. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <application
        android:label="EDV Route"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Tapping a push opens the app. The backend sends this same
                 click_action; without the pair, the tap does nothing. -->
            <intent-filter>
                <action android:name="FLUTTER_NOTIFICATION_CLICK"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <!-- Android 8+ refuses to show a notification with no channel. Declaring
             the default here means a push that arrives with the app CLOSED (when
             no Dart code has run to create one) is still drawn. Must match the
             channelId the backend sends. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="edv_avisos"/>
        <!-- flutter_foreground_task's service. `location` is mandatory from
             Android 14: a foreground service with an undeclared type is refused
             at start, and the failure surfaces as "tracking simply never runs". -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher: open the signed document URL (PDF) in the system viewer/browser. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
