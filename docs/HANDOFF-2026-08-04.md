# HANDOFF — EDV Route · continuación (2026-08-04)

> Documento para retomar en una ventana de contexto nueva. Léelo **completo** antes de tocar nada.
> Reglas del usuario (Luis) en `C:\Users\luisv\.claude\CLAUDE.md`: chat/explicaciones en **español**,
> código/comentarios en **inglés**; si el prompt termina en `?` = modo solo-lectura; ser directo y
> crítico; avisar antes de codear algo que viole SoC/DRY. Monorepo en `C:\Project\edv\`.

---

## ⚠️ PENDIENTE CRÍTICO 2026-08-05 — Paso 4 (PAGO) del registro NO replica el admin

**Estado:** el Paso 4 (pago) de la app sigue con el patrón de COLECCIÓN ("＋ Agregar pago" → modal
`payment_draft_sheet` → tarjeta con Editar/Quitar), que sugiere varios pagos cuando es UNO solo. **No
muestra el resumen de cobro (el chofer no ve cuánto paga) ni permite pagar semanas adelantadas.**

**Lo que Luis exige (aprobado el 2026-08-05, NO implementado):** el Paso 4 debe ser **EXACTO al admin,
salvo Efectivo Divisa** (eso sí es admin-only). Replicar el Paso 4 del wizard del admin:
1. **Resumen de cobro**: membresía + tarifa (plan) + **total en USD** visible.
2. **Selector de plan + semanas adelantadas** (pagar N semanas por adelantado), como el admin.
3. **Captura de pago INLINE** (un solo pago, formulario en la pantalla, no modal ni lista), espejando
   `edv-route-admin/src/app/features/drivers/payment-capture.html`.
- Referencia admin: `driver-wizard.html` Paso 4 (Resumen de cobro + plan/semanas) + `payment-capture.html/ts`.

**Implicación de BACKEND (no es solo UI):** hoy el registro de la app emite el alta como **DEUDA**
(membresía + 1 semana) y el pago va como submission `purpose='debt'` — `/driver-auth/payment-submissions`
**fuerza `purpose='debt'` y `amountUsd=null`**, y el backend deriva el monto. Para replicar el admin
(elegir plan + semanas adelantadas + mostrar total), el backend de la app debe soportar el flujo **enroll
con plan/semanas** (como `settleAdvanceOnClient`/enroll del admin), no solo debt. → cambio de **backend +
app**. Analizar el contrato del admin antes de implementar.

**Error de proceso a NO repetir:** el 2026-08-05 se generó un APK afirmando que el Paso 4 estaba "rehecho
inline" — FALSO, no se implementó (el APK llevaba el patrón viejo). No declarar hecho lo no implementado
ni omitir lo que Luis pidió explícitamente ("exacto al admin salvo divisa").

---

## 0. TAREA PRIORITARIA (lo que Luis exige AHORA, con urgencia)

El **registro de chofer en la app Flutter** (`edv-route-mobile`) quedó **incompleto y desalineado**
respecto al registro del **admin** (`edv-route-admin`). Luis lo probó en el teléfono y falta trabajo:

### 0.1 El registro de la app debe ser IDÉNTICO al del admin — mismos campos, mismo patrón, mismo orden
El wizard de la app (`lib/features/auth/presentation/screens/driver_register_screen.dart`, Paso 1
"Datos personales") **NO tiene todos los campos del admin**. Faltantes / diferencias detectados por Luis:
- **Falta la fecha de nacimiento** (`birthDate`). El admin la pide (date picker, valida ≥18 años).
- **Falta la confirmación de contraseña** (repetir clave). El admin la tiene.
- **La cédula/teléfono se manejan diferente**: el admin usa selectores propios:
  - Cédula: selector V/E/J + dígitos (5-9). En la app existe `NationalIdField` (parecido) pero hay que
    verificar que coincida EXACTO con el admin.
  - **Teléfono con selector de OPERADORA** (`0412/0422/0414/0424/0416/0426`, se compone a `+58`+10). La
    app hoy usa un campo libre de 10-11 dígitos → **NO** coincide con el admin.
- Hay que **leer el `person-form` del admin y replicar campo por campo** (etiquetas, orden, validaciones,
  obligatoriedad, formatos canónicos). **Referencia admin**:
  `edv-route-admin/src/app/features/drivers/person-form.ts` (`composePerson()` ~:129-228; patrones
  `NAME_VALID`, `NATIONAL_ID_OPTIONS`, `PHONE_OPERATOR_OPTIONS`; `birthDate` ≥18; documento y password
  obligatorios en registro).
- **Vehículo y pago**: el paso de vehículo (`vehicle-form.ts`/`vehicle-draft-modal.ts`) y el de pago
  (`payment-capture.ts`) del admin también son la referencia. El PAGO ya se alineó bastante (selector de
  banco venezolano + datos del destino), pero **revisar que TODO coincida** con el admin.

**Objetivo**: que un chofer vea en la app los mismos campos, en el mismo orden, con las mismas reglas que
un admin ve en el panel. Leer los componentes del admin y espejarlos con estilo Flutter.

### 0.2 El HEADER de la app se ve mal — normalizarlo al global
Luis mandó 2 capturas: el header de **login/registro** ("Ingresar como conductor") se ve **MAL**; el de
**selección de modo** ("¿Qué modo deseas usar?", con "modo" en dorado) es el **global correcto** que se
debe reutilizar en todas las pantallas.
- Ambas pantallas usan el MISMO componente `lib/shared/widgets/auth_header.dart` (→ `gradient_header.dart`).
- En esta sesión **reduje la altura del `AuthHeader`** (logo 44→38, padding `showBack ? 8 : 18` / bottom
  30→20, título 26→23) para "compactarlo" — **es posible que eso lo haya empeorado**. El nuevo agente
  debe comparar login (showBack=true) vs selección (showBack=false, con `highlight` dorado) y hacer que
  **todas las pantallas** (incl. registro) usen el header que se ve bien (imagen 2). Probablemente:
  revisar/ajustar el padding cuando `showBack`, o revertir la reducción y buscar otra forma de compactar
  sin romper el look. **El registro debe usar ese mismo header global**, no una variante.

### 0.3 (URGENTE) Aprobación del pago del alta — Luis no encuentra dónde aprobar el pago del registro
Tras el fix del Efectivo Divisa, el pago SÍ se registra como **submission pendiente** (el perfil muestra
banda "Pago en revisión · $225.00"). PERO la factura del alta (#123 = deuda del alta, $185) aparece **sin
datos de pago** ("Esta factura no registra datos de pago", "Sin comprobante") — eso es el comportamiento
de v9 (la factura se estampa AL APROBAR el submission). El problema de Luis: **no encuentra dónde ver los
datos del pago ($225, efectivo, fotos) ni cómo aprobarlo**; "no me deja aprobar ese pago".
- El submission se aprueba en **Facturación → pestaña "Por aprobar"** (`billing.ts` tab `'review'`;
  `billing.html:69-77`) → ruta `/billing/submissions/:id` (`payment-submission-detail`, muestra las fotos)
  → Aprobar. El submission SÍ aparece ahí (sin filtro que lo excluya); el problema era **descubribilidad**.
- ✅ **HECHO (admin `29256d4`)**: la banda "Pago en revisión" del perfil (`driver-detail.html:113-120`)
  ahora tiene botón **"Ir al pago para aprobarlo"** → `/billing/submissions/:id` (usa `ps.id` de
  `pendingSubmission`). RouterLink ya estaba importado.
- 🔴 **Hallazgos del diagnóstico AÚN por decidir/arreglar** (verificados, backend+admin):
  1. **Discrepancia $225 vs $185**: la factura del alta (#123) es la deuda ($185); el submission cash es el
     efectivo recibido ($225). Al aprobar (`enrollment.repository.ts:637-722` `settleDebtOnClient`) la
     factura queda en **$185** (suma de cargos, no del efectivo) y **el excedente de $40 se pierde en
     silencio** (no crédito, no adelanto, no traza). DECIDIR con Luis la regla del excedente cash.
  2. **Comprobante NO se copia a la factura**: el módulo `payment-submissions` no toca `proof_url` → la
     factura muestra "Sin comprobante" aunque el submission tiene fotos. Copiar/vincular en `approve`
     (`payment-submissions.repository.ts:370-381`).
  3. **Botón "Aprobar" del perfil aprueba el CHOFER, no el pago** (`drivers.service.ts:582-600`); con pago
     pendiente, `assertApprovable` (`:566-579`) lo bloquea con error confuso de "deuda". Desambiguar/mejorar
     el mensaje cuando hay `pendingSubmission`.
  4. **Historial del perfil** (`driver-payments.ts:126-152`) no lista submissions pendientes; el template
     (`driver-payments.html:30-33`) imprime "Pagado el {fecha}" incluso para "Pendiente". Corregir.
- Chofer de prueba actual: **Nazareth Nohemi Ceballos Cedeño (V-33150678)**, submission $225 pendiente.

### 0.4 Opcionales / recomendaciones abiertas
- **Fallo silencioso del wizard del admin**: `driver-wizard.ts` register() usa
  `catchError(() => paymentSent: false)` → si el pago falla, NO avisa. Recomendado mostrar el error.
  (Ya se lo dije a Luis; falta que confirme.)
- **Paso 5 "Resumen"** en la app (el admin tiene 5 pasos: Datos, Documentos, Vehículo, Pago, Resumen; la
  app tiene 4 y envía desde Pago). Luis lo mencionó pero no confirmó. Preguntar.
- **Smoke test end-to-end de la app**: aún NO se probó un registro completo en vivo desde el teléfono con
  éxito (el 1er intento falló por bugs ya arreglados). Falta confirmar que cierra de punta a punta.

---

## 1. Panorama del proyecto

EDV Route = plataforma para choferes. Tres proyectos en `C:\Project\edv\`:
- `edv-route-backend` — Fastify 5 + TS ESM + PostgreSQL/PostGIS (Supabase). Repo:
  `github.com/edv-route/edv-route-backend` (rama `main`). Desplegado en **Railway** (deploy auto al pushear).
- `edv-route-admin` — Angular 22 + Tailwind 4 + Flowbite. Repo: `github.com/edv-route/edv-route-admin`
  (rama `main`). Desplegado en Railway (Docker/Caddy).
- `edv-route-mobile` — **Flutter 3.35.7 / Dart 3.9.2**, Material 3. App de choferes (login + registro).
  **SIN repo git aún** (queda local). Apunta por defecto a Railway (prod). Package `com.edvroute.edv_route_mobile`.

**Estado del ecosistema del registro (meta = registrar chofer desde la app):** backend COMPLETO y
desplegado; la app tiene login + wizard de registro CONSTRUIDO pero con los problemas de la sección 0.

---

## 2. Lo logrado en esta sesión (todo verde y desplegado salvo lo de la sección 0)

### 2.1 Backend (`edv-route-backend`) — COMPLETO, en `main`, desplegado
Registro del chofer desde la app + limpieza. Sin migración (columnas ya existían). `typecheck` verde,
suite **30/30**. Commits en `main`:
- `687be33` — feat: registro de chofer desde la app + limpieza de solicitantes.
- `80edf93` — fix: `submitted_by` null en el envío de pago de la app (era FK a `admins`; el chofer no es
  admin → violaba la FK; el actor va en `audit_logs.actor_user_id`).
- `385f830`/anteriores — v9 (base, ya estaba).

Qué incluye (archivos backend):
- `writeAudit` (`src/modules/audit-logs/audit-writer.ts`) acepta `actorUserId` (FK `users`).
- `DriversService.register` (`src/modules/drivers/drivers.service.ts`) parametrizado por `source`
  (`'admin'|'app'`): en `app`, `registered_by`/`uploaded_by` = null y actor en `actor_user_id`.
  Backward-compatible (default admin).
- `POST /driver-auth/register` (público) + rutas driver-audience bajo **`/driver-auth`** (confirmado por
  Luis, no `/driver`): `/driver-auth/payment-submissions`, `/driver-auth/documents/:id/file`,
  `/driver-auth/vehicles/:vehicleId/images` — con verificación de propiedad (recurso = `req.user.sub`).
  Todo en `src/modules/driver-auth/` + `src/modules/drivers/drivers.schemas.ts` (schemas compartidos
  admin+app, extraídos).
- `applicant-cleanup-scheduler` (`src/plugins/applicant-cleanup-scheduler.ts`): borra `pending` sin pago
  vivo (>7d) y `rejected` (>7d). **Dry-run por defecto** (flag `applicant_cleanup_enabled` en `app_settings`;
  no existe la fila → false → solo loguea).
- Docs backend al día: `docs/api/endpoints.md`, `docs/decisions/decisions-log.md` (entrada 2026-08-04),
  `docs/proposals/pagos-aprobacion/README.md`.

### 2.2 App Flutter (`edv-route-mobile`) — wizard construido, `flutter analyze` verde, APK generado
Arquitectura por capas (pantalla → controller `ChangeNotifier` → repository → data source → `ApiClient`).
DI manual en `lib/core/di.dart`. Lo construido:
- **Capa de datos del registro**: `lib/features/auth/domain/repositories/registration_repository.dart`
  (+ `PaymentCapture`), `.../data/repositories/registration_repository_impl.dart`,
  `.../data/datasources/registration_remote_data_source.dart`, DTOs `.../data/models/register_request.dart`
  (`RegisterRequest`, `VehicleDraft`, `DocumentRef`, `RegisterResult`, `CreatedVehicle`), entidades
  `Requirement`, `PaymentMethodOption`, `PickedImage`. `ApiClient` ganó `postMultipart` + `getList`.
- **Wizard 4 pasos**: `lib/features/auth/presentation/screens/driver_register_screen.dart` +
  `.../controllers/driver_register_controller.dart` + `.../screens/registration_done_screen.dart`.
  Paso 1 datos, Paso 2 documentos (foto/PDF con cámara/galería/archivo), Paso 3 vehículo (datos + docs +
  fotos), Paso 4 pago (método + fecha + comprobante + datos del pagador). Envío: `_submit()` → `register`
  → subir fotos (por orden de `createdDocumentIds`/`createdVehicles`) → `submitPayment` → pantalla "en
  revisión".
- **Pago alineado (parcial)**: `lib/features/auth/data/payment_catalogs.dart` (`venezuelanBanks` +
  `destinationRows`); selector de banco (bottom sheet); tarjeta de datos del método destino; campos del
  pagador por tipo.
- Ajustes: nombres en Title Case (`_titleCase`), placa MAYÚSCULAS + validación `^[A-Z0-9]{5,8}$`,
  `BrandTextField` ganó `textCapitalization`. Header compactado (VER 0.2 — puede haber quedado mal).
  Botón sin sombra (`primary_button.dart`). Icono de app con `flutter_launcher_icons` (logo sobre rojo).
- Deps añadidas: `image_picker`, `file_picker`, `flutter_launcher_icons`.
- **APK**: `flutter build apk --release` → `build/app/outputs/flutter-apk/app-release.apk` (~48 MB), apunta
  a Railway. Enviado a Luis.

### 2.3 Fix en el admin (`edv-route-admin`) — en `main`, desplegado
- `a9bdc86` — fix(pagos): el wizard reenvía `amountUsd` para Efectivo Divisa. **Era un bug de v9, NO de
  esta sesión** (`git log -S amountUsd -- driver-wizard.ts` = vacío). `buildDebtForm`
  (`src/app/features/drivers/driver-wizard.ts:411`) no mandaba `amountUsd`; el backend lo exige para
  `cash_usd` → la submission se rechazaba y el `catchError` la tragaba → chofer registrado sin pago.
  Añadido `if (draft.amountUsd) form.set('amountUsd', draft.amountUsd);`. Build (`ng build`) OK.

---

## 3. Archivos clave (rutas)

**App (a corregir):**
- `edv-route-mobile/lib/features/auth/presentation/screens/driver_register_screen.dart` — el wizard (aquí
  faltan campos del Paso 1; añadir birthDate + confirmación de password + teléfono con operadora).
- `.../presentation/controllers/driver_register_controller.dart` — estado del wizard + `submit()`.
- `.../data/models/register_request.dart` — DTO (ya tiene `birthDate` en `RegisterRequest`; el UI no lo
  captura).
- `edv-route-mobile/lib/shared/widgets/auth_header.dart` + `gradient_header.dart` — el HEADER (normalizar).
- `.../shared/widgets/brand_text_field.dart`, `password_field.dart`, `primary_button.dart` — widgets base.
- `.../features/auth/presentation/widgets/national_id_field.dart` — selector cédula V/E/J.
- `edv-route-mobile/lib/features/auth/data/payment_catalogs.dart` — bancos + campos destino.

**Admin (la REFERENCIA a espejar):**
- `edv-route-admin/src/app/features/drivers/person-form.ts` (+ `.html`) — datos personales (birthDate,
  operadora de teléfono, confirmación password, todos los campos y validaciones).
- `.../drivers/driver-wizard.ts` (+ `.html`) — los 5 pasos, orden, `register()`.
- `.../drivers/vehicle-form.ts` / `vehicle-draft-modal.ts` — vehículo (titleCase marca/modelo/color, placa
  uppercase).
- `.../drivers/payment-capture.ts` (+ `.html`) — captura de pago (método, campos por tipo, banco).
- `.../core/models/payment-method.model.ts` — `VENEZUELAN_BANKS`, `PAYMENT_METHOD_FIELDS`.
- `.../shared/components/select` — el `app-select` (dropdown branded del admin).

**Backend (referencia, ya desplegado):**
- `edv-route-backend/src/modules/driver-auth/driver-auth.routes.ts` / `.service.ts` / `.schemas.ts`.
- `.../modules/payment-submissions/payment-submissions.service.ts` (flujo cash: `methodIsCash`, exige
  `amountUsd`).
- `.../modules/drivers/drivers.service.ts` (`register`), `drivers.schemas.ts`.

---

## 4. Comandos clave

| Dónde | Comando | Qué |
|---|---|---|
| app | `Push-Location C:\Project\edv\edv-route-mobile; flutter analyze; Pop-Location` | Verificar la app (rápido) |
| app | `Push-Location C:\Project\edv\edv-route-mobile; flutter build apk --release; Pop-Location` | Generar APK (~1-3 min) |
| app | correr en emulador (NO botón Gradle): `flutter run --dart-define=API_BASE_URL=http://10.0.2.2:3000/api/v1 -d emulator-5554` | Probar contra backend local |
| backend | `npm --prefix C:\Project\edv\edv-route-backend run typecheck` | Typecheck (seguro) |
| backend | `npm --prefix C:\Project\edv\edv-route-backend test` | Suite (⚠️ escribe en la BD dev + apaga `debt_engine_enabled`) |
| admin | `Push-Location C:\Project\edv\edv-route-admin; npm run build; Pop-Location` | Build (verifica compilación) |
| enviar APK a Luis | `SendUserFile` con `build/app/outputs/flutter-apk/app-release.apk` | — |

---

## 5. Gotchas / avisos importantes

- **Están en DESARROLLO, no producción real.** La BD Supabase es de dev; se puede modificar sin drama.
  Luis reenciende `debt_engine_enabled` si la suite lo apaga: `UPDATE app_settings SET value='true'::jsonb
  WHERE key='debt_engine_enabled';` (Luis lo ejecuta en SU Supabase; el MCP de Supabase de esta sesión
  apunta a OTRO proyecto `reintegracion22`, NO a la BD de EDV → no se puede consultar/escribir la BD de
  EDV por MCP).
- **La app NO tiene repo git** — no se pushea; se comparte el APK. El backend y el admin SÍ tienen repo
  (`main`) y se despliegan solos en Railway al pushear.
- **No pushear sin que Luis lo pida.** Backend y admin ya están al día (`main` limpio).
- El registro de la app crea el chofer en el paso final (`_submit`), no en el camino; el backend crea el
  chofer ANTES del pago (2 endpoints, no atómico) — igual que el admin. Si el pago falla tras `register`,
  el chofer queda a medias (`pending`); reintento/reanudación NO automatizado (MVP).
- Chofer de prueba fallido: **Luis Dario Villegas Vargas (V-27357951)** quedó registrado con deuda $185 sin
  pago (por el bug del efectivo, ya arreglado). Para reprobar, usar OTRA cédula o borrarlo.
- Al probar la app: usar cédula NUEVA (≠ `V-22198958`, que ya existe como chofer de prueba de login).

---

## 6. Estado en una línea
Backend del registro: **hecho + desplegado**. App: **wizard construido pero desalineado del admin**
(faltan birthDate + confirmación password + teléfono con operadora; header mal) — **ESA es la tarea**.
Fix del Efectivo Divisa del admin: **hecho + desplegado** (`a9bdc86`).
