# HANDOFF — App del chofer, sesión 2026-08-17

> Continúa `HANDOFF-2026-08-14.md`. Cubre: **pantalla de espera** post-aprobación, el **PERFIL del
> afiliado** completo (con catálogo/detalle de vehículo + carrusel de fotos), y ajustes de UX
> (**isla flotante** en la barra inferior, chip Activo/Inactivo, fix del notch, nombre de la app).
>
> **Backend y panel: TODO desplegado a prod.** **App: trabajo NUEVO de esta sesión SIN commitear**
> sobre el commit local `6908448` (el repo del móvil sigue **sin remoto**).

## 0. Estado en una línea
El afiliado ve una **pantalla de espera** si aún no tiene fecha de inicio de tarifa, y tiene un
**perfil real** (estado de cuenta, documentos y vehículos navegables con fotos, beneficios, datos),
con la **barra inferior en isla flotante roja**. Falta: smoke E2E en vivo, el **estado de cuenta
completo** (próximo pago / cobertura / penalizado) y el **módulo de Viajes**.

## 1. Backend (`edv-route-backend`) — TODO DESPLEGADO a prod (Railway)
- `7a38c1d` — `GET /driver-auth/documents/:id/file`: URL firmada (60 s) para previsualizar el archivo
  **propio** (auth chofer + ownership).
- `b6f1a34` — **Forma A**: el pago `debt` acepta `periods` (adelanto de semanas del alta). Cubre la
  deuda base + `periods−1` semanas extra, **creadas pagadas al APROBAR** el recibo (sin deuda fantasma
  si se rechaza). `enrollment.addPaidAltaWeeksOnClient`.
- `1fa24af` — **`tariffStarted`** en el perfil (`/me` + login) = `tariff_start_set_at IS NOT NULL`.
- `0cd4f06` — `GET /driver-auth/me/vehicles`: vehículos con detalle completo (brand/model/year/color/
  plate/vehicleType/approvalStatus/rejectionReason) + **fotos como URLs firmadas (60 s)**.
- Docs al día: `docs/api/endpoints.md` + `docs/decisions/decisions-log.md`.

## 2. Panel admin (`edv-route-admin`) — DESPLEGADO a prod
- `91f1416` — candado de veredicto: `Aprobar`/`Rechazar` solo en «En revisión»; aprobado/rechazado deja
  solo `Ver`. Se reabre a `pending` cuando el chofer resube.

## 3. App (`edv-route-mobile`) — base `6908448` + TRABAJO NUEVO **SIN COMMITEAR**
Rama `main`, **sin remoto**. Todos los cambios de esta sesión están en el **working tree** (ver §4).

### 3.1 Pantalla de espera post-aprobación
- `alta_payment_screen.dart`: si `approved` + deuda 0 + **`!tariffStarted`** → `_WaitingStartState`
  («¡Fuiste aprobado! Falta que la oficina te active») en vez del home. Solo aparece **mientras no tiene
  fecha de inicio**; con «empezar ya» pasa al home; con «próximo lunes» queda `scheduled` (por ahora
  también entra al home, el reloj/cuenta regresiva es futuro).
- `driver.dart` + `driver_dto.dart`: nuevos campos **`tariffStarted`** y **`avgRating`**.

### 3.2 Perfil del afiliado (`features/home`)
- `profile_screen.dart` **reescrito**: cabecera (avatar, nombre, cédula, **valoración**, chip
  **Activo verde / Inactivo naranja** por `isAvailable`) · **Estado de cuenta** (`/me/debt`: al día /
  debes + botón Pagar → `AltaPaymentScreen`) · **Documentos** y **Vehículos** navegables (reutilizan
  `documents_list`/`vehicles_list` en lectura) · **Beneficios** (`/membership`) · datos · cerrar sesión.
  El **header va FIJO fuera del scroll** → arregla el solape del notch/barra de estado al hacer scroll.
- `profile_controller.dart` **(nuevo)**: carga `/me/debt` + `/membership`.
- `vehicles_list_screen.dart`: flag **`allowAdd`** (false en el perfil → oculta «Agregar vehículo»).

### 3.3 Detalle del vehículo con carrusel (Fase 2)
- `vehicle_full.dart` **(nuevo)**: modelos `VehicleFull` + `VehicleImage`.
- `registration_remote_data_source.dart` / `registration_repository.dart`(+ impl): **`loadVehicles()`**
  → `GET /me/vehicles`.
- `vehicle_detail_screen.dart` **reescrito**: **carrusel** de fotos (`PageView` + puntos) + **info
  completa** (marca/modelo, año, color, tipo, placa) + documentos navegables (del checklist). Carga
  `/me/vehicles` en `initState` y cruza con el `ChecklistVehicle` para los documentos.

### 3.4 Ajustes de UX
- **Isla flotante** en la barra inferior (opción 2 elegida por Luis): `driver_shell.dart` con
  `_FloatingNav` — pastilla **roja** despegada de los bordes, tab activo resaltado en **dorado**,
  animación al cambiar. Hoy 2 tabs (Inicio / Perfil); crecerá (Solicitudes/Viajes).
- **Nombre de la app → «EDV Route»** (`AndroidManifest.xml` `android:label`).

### 3.5 Tests (14 verdes)
- `golden_test.dart`: **se quitaron** los goldens de `driver_home` / `driver_profile` (ahora cargan
  datos por red → no son golden-ables sin un repositorio falso; TODO). PNGs borrados. Quedan
  `selection` + `driver login` + `widget_test` (selección) + `checklist_test` (11 casos de dominio).

## 4. Estado de git del móvil (SIN COMMITEAR, sobre `6908448`)
```
 M android/app/src/main/AndroidManifest.xml
 M lib/features/auth/data/datasources/registration_remote_data_source.dart
 M lib/features/auth/data/models/driver_dto.dart
 M lib/features/auth/data/repositories/registration_repository_impl.dart
 M lib/features/auth/domain/entities/driver.dart
 M lib/features/auth/domain/repositories/registration_repository.dart
 M lib/features/auth/presentation/screens/alta_payment_screen.dart
 M lib/features/auth/presentation/screens/vehicle_detail_screen.dart
 M lib/features/auth/presentation/screens/vehicles_list_screen.dart
 M lib/features/home/presentation/screens/driver_shell.dart
 M lib/features/home/presentation/screens/profile_screen.dart
 M test/golden_test.dart
 D test/goldens/driver_home.png
 D test/goldens/driver_profile.png
?? lib/features/auth/domain/entities/vehicle_full.dart
?? lib/features/home/presentation/controllers/profile_controller.dart
```
`flutter analyze` limpio · `flutter test` 14/14 · último APK entregado a Luis (~50 MB, apunta a prod).
Luis prefirió **probar antes de commitear** → cuando dé el visto bueno, un solo commit con todo esto.

## 5. PENDIENTE (por dónde seguir)
1. **Smoke E2E en vivo** de lo nuevo: pantalla de espera, perfil, vehículo con fotos, isla flotante, y
   el **rechazo** de un pago con semanas de la Forma A (que NO quede deuda fantasma).
2. **Estado de cuenta COMPLETO** en el perfil (lo que quedó de la lista original): **próximo pago**
   (cuándo/cuánto), **hasta cuándo está cubierto**, estado **penalizado**. La lógica ya existe en el
   admin (`drivers.repository.ts` → `weeklyNextChargeAt` :461-489, y `findDetail` → `upcoming` :352-365
   / `subscription.paidUntil` :366-382). **Falta**: exponer un `GET /driver-auth/me/account` scoped por
   token que lo devuelva, y consumirlo en `profile_screen._accountSection`.
3. **Módulo de Viajes** (historial + valoraciones detalladas): proyecto propio, aún NO existe. Iría en
   el home/dashboard; incluye el **reloj / cuenta regresiva** para el afiliado con inicio **programado**
   (`scheduled`, «próximo lunes»), que sale hasta que arranca y luego desaparece.
4. **`DriverStatus.fromApi`** de la app **no mapea** `overdue/penalized/paused/scheduled` (colapsan a
   `unknown`). Ampliar el enum cuando toque el estado de cuenta y el estado `scheduled`.
5. **Definir el repo remoto del móvil** y **commitear** todo lo de §4.
6. Copys/detalles: el header de `AltaPaymentScreen` dice «Paga tu alta» también en la pantalla de espera
   (mejorar). Pantalla de **T&C real** (hoy checkbox), «olvidé mi contraseña».

## 6. Gotchas
- **prod = dev, misma BD** (Supabase, 15 conexiones): no correr dos backends a la vez.
- Chofer de prueba **operando**: `V-22198958 / 123456` (approved).
- **Goldens** sensibles al entorno (regenerados en la máquina de Luis).
- El detalle de vehículo y la vista previa de documentos usan **URLs firmadas de 60 s** (recargar si
  expiran).

## 7. Línea de contexto para retomar
> Retomamos EDV Route (C:\Project\edv). Lee `edv-route-mobile/docs/HANDOFF-2026-08-17.md`. Backend
> (`0cd4f06`) y panel (`91f1416`) están **en prod**; el móvil tiene el **perfil del afiliado + pantalla
> de espera + vehículo con carrusel + isla flotante** implementados y **SIN commitear** sobre el commit
> local `6908448` (repo sin remoto) — `flutter analyze` limpio, 14 tests verdes, APK entregado. El
> **siguiente paso** acordado es el **estado de cuenta completo** del perfil (próximo pago / hasta
> cuándo cubierto / penalizado): crear `GET /driver-auth/me/account` reutilizando `weeklyNextChargeAt`
> + `findDetail.upcoming/subscription` del canal admin, y consumirlo en `profile_screen`. Ojo: móvil
> sin commitear (Luis quería probar antes) y gotcha de conexiones (prod=dev, no correr dos backends).
